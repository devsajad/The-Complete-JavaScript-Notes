## Setting up prettier and VS code

1. you can config prittier by creating file named .prittierrc
2. you can create your snippets : file > preferences > snippets
3. todo Highlight plugin (to highligh specific words like BUG , ... ) : file > settings > JSON

## Debugging with console

```JS
console.table();// Create table from array or obejects
console.warn("test warn");
console.error("test error");
```

we can call debugger from the code 
```js
debugger;
console.log();
```
